public class Admin extends Account {

    // Constructor untuk Admin
    public Admin(String id, String userName, String password) {
        super(id, userName, password); // Memanggil konstruktor dari superclass (Account)
    }
}
